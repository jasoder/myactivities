[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "myactivities"
version = "0.1.0"
description = "FastAPI backend for tracking athlete activities"
requires-python = ">=3.11"
dependencies = [
    "fastapi", 
    "uvicorn", 
    "sqlalchemy",
]